const DOC_PATH = '/doc';

const CONTENT_PATH = '/content';

const DRAWER_PATH = `${DOC_PATH}${CONTENT_PATH}/drawer`;
const SWITCH_PATH = `${DOC_PATH}${CONTENT_PATH}/switch`;

export default { DOC_PATH, CONTENT_PATH, DRAWER_PATH, SWITCH_PATH };
